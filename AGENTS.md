# AGENTS.md — 零台入口

> 任何 AI 编程助手读到本文件后，应按照灵台协议操作本知识库。

## 这是什么

灵台（Lingtai），基于 Obsidian 的 AI 知识管理系统。核心理念：**AI 整理弹药，人扣扳机。**

## AI 启动协议

新 LLM 会话接入后，按顺序读取：

1. `CLAUDE.md` — 操作规范（必读，这是唯一信标（优先于 AGENTS.md）
2. `索引.md` — 全库导航
3. `入门/灵台用户画像.md` — 若存在，融入用户角色；不存在则以纯引擎模式运行

### 首次使用："开始"

用户说「开始」时，执行首次设置流程：

1. 检查 `丹房/` 和 `原料/` 目录是否存在
   - **不存在** → 提示用户运行 `python scripts/lingtai.py init`，询问其职业（内容创作者/程序员/商业/自定义），引导选择对应模板
   - **已存在** → 欢迎用户，引导扔原料或填写画像
2. 打印简短欢迎语和下一步

## 触发词 → 入口

| 触发 | 入口 |
|:----|:----|
| 提炼/ingest | `CLAUDE.md §三` + `巡更/自动提炼.md` |
| 问知/query | `索引.md` + 目标丹房页 |
| 体检/lint | `python scripts/lint_check.py` |
| 产出 | 见 `输出/` 下各 skill 文件 |

## 灵识 MCP（可选增强）

零台支持通过灵识 MCP Server 获得增强能力（知识检索、图谱分析、自动观察等）。

**接入方式**：将 `.tool/lingshi/mcp_server.py` 配置为 MCP Server，AI 客户端即可调用 29 个工具。

详见 `.tool/lingshi/README.md`。

## 架构

```
灵台/
├── CLAUDE.md              ← 系统宪法（唯一信标（优先于 AGENTS.md）
├── AGENTS.md            ← 本文件（AI 入口）
├── 索引.md              ← 全量目录
├── 日志.md              ← 操作日志
│
├── 丹房/                ← 提炼后的知识 Wiki
│   ├── 01-XX/           ← 按主题分域
│   └── ...
│
├── 原料/                ← 原始素材，只读不改
│   └── 附件/
│
├── 体检/                ← 健康检查报告
├── 入门/                ← 用户指南 + 画像
├── 巡更/                ← 自动化规范
├── 输出/                ← 内容产出框架
├── scripts/             ← 自动化脚本
└── .tool/lingshi/       ← 灵识 MCP（可选）
```

## 核心约束

- 不编造：内容可追溯到原料原文
- 原料只读：仅标注处理状态，不改内容
- 融入优先：新知识融入已有页面，全新概念才建新页
- 操作收尾：写日志 → git commit

## 依赖

- Obsidian（推荐）
- Python 3.10+（体检脚本）
- 任意 AI 助手（WB/Claude/GPT/Copilot 均可）
